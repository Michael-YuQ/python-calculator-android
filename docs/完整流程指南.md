# 📱 Python 转 Android APK - 完整流程指南

> 从零开始的详细教程，包含所有步骤和常见问题解答

---

## 📋 目录

1. [环境准备](#环境准备)
2. [本地开发](#本地开发)
3. [Colab 构建](#colab-构建)
4. [安装测试](#安装测试)
5. [自定义配置](#自定义配置)
6. [常见问题](#常见问题)

---

## 环境准备

### 需要的工具

- ✅ **GitHub 账号** - 用于托管代码
- ✅ **Google 账号** - 用于访问 Colab
- ✅ **Git** - 用于版本控制
- ✅ **文本编辑器** - VS Code、PyCharm 等

### 不需要的东西

- ❌ Android Studio
- ❌ Java JDK（本地）
- ❌ Android SDK（本地）
- ❌ 复杂的环境配置

---

## 本地开发

### 1. 创建项目

如果是新项目：

```bash
# 克隆模板
git clone https://github.com/Michael-YuQ/python-calculator-android.git
cd python-calculator-android

# 或者从头开始
mkdir my-android-app
cd my-android-app
git init
```

### 2. 编写应用代码

创建或编辑 `main.py`：

```python
from kivy.app import App
from kivy.uix.boxlayout import BoxLayout
from kivy.uix.button import Button
from kivy.uix.textinput import TextInput
from kivy.uix.label import Label


class CalculatorApp(App):
    def build(self):
        # 主布局
        main_layout = BoxLayout(orientation='vertical', padding=10, spacing=10)
        
        # 显示屏
        self.display = TextInput(
            text='0',
            readonly=True,
            halign='right',
            font_size=48,
            size_hint=(1, 0.3)
        )
        main_layout.add_widget(self.display)
        
        # 按钮布局
        buttons = [
            ['7', '8', '9', '/'],
            ['4', '5', '6', '*'],
            ['1', '2', '3', '-'],
            ['C', '0', '=', '+']
        ]
        
        for row in buttons:
            h_layout = BoxLayout(spacing=5)
            for label in row:
                button = Button(
                    text=label,
                    font_size=32,
                    on_press=self.on_button_press
                )
                h_layout.add_widget(button)
            main_layout.add_widget(h_layout)
        
        return main_layout
    
    def on_button_press(self, instance):
        current = self.display.text
        button_text = instance.text
        
        if button_text == 'C':
            self.display.text = '0'
        elif button_text == '=':
            try:
                self.display.text = str(eval(current))
            except:
                self.display.text = 'Error'
        else:
            if current == '0':
                self.display.text = button_text
            else:
                self.display.text = current + button_text


if __name__ == '__main__':
    CalculatorApp().run()
```

### 3. 本地测试（可选）

```bash
# 安装 Kivy
pip install kivy

# 运行测试
python main.py
```

### 4. 配置构建文件

确保有 `buildozer.spec` 文件：

```ini
[app]
title = My App
package.name = myapp
package.domain = org.example

source.dir = .
source.include_exts = py,png,jpg,kv,atlas

version = 1.0

requirements = python3,kivy

orientation = portrait
fullscreen = 0

android.permissions = 
android.api = 33
android.minapi = 21
android.ndk = 25b
android.accept_sdk_license = True
android.archs = arm64-v8a

[buildozer]
log_level = 2
warn_on_root = 1
```

### 5. 提交到 GitHub

```bash
# 添加文件
git add main.py buildozer.spec requirements.txt

# 提交
git commit -m "Initial commit"

# 推送到 GitHub
git remote add origin https://github.com/你的用户名/你的仓库名.git
git push -u origin main
```

---

## Colab 构建

### 1. 打开 Google Colab

访问：https://colab.research.google.com/

### 2. 创建新笔记本

点击 "新建笔记本" 或 "File" → "New notebook"

### 3. 复制构建脚本

将以下完整脚本粘贴到单元格：

```python
"""
Python 转 Android APK - Colab 构建脚本
"""

# ========================================
# 配置区域（修改这里）
# ========================================

# 你的 GitHub 仓库地址
GITHUB_REPO = "https://github.com/Michael-YuQ/python-calculator-android.git"

# 仓库名称（URL 最后的部分）
REPO_NAME = "python-calculator-android"

# ========================================
# 以下代码无需修改
# ========================================

print("🚀 开始构建 Android APK...")
print(f"📦 仓库: {GITHUB_REPO}")
print("⏱️  预计时间: 20-30 分钟\n")

# 步骤 1: 安装 Python 依赖
print("📥 [1/5] 安装 Python 依赖...")
!pip install -q buildozer cython
print("✅ Python 依赖安装完成\n")

# 步骤 2: 克隆项目
print("📥 [2/5] 克隆项目...")
!git clone {GITHUB_REPO}
%cd {REPO_NAME}
print("✅ 项目克隆完成\n")

# 步骤 3: 安装系统依赖
print("📥 [3/5] 安装系统依赖...")
!sudo apt-get update -qq
!sudo apt-get install -y -qq git zip unzip openjdk-17-jdk wget autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev
print("✅ 系统依赖安装完成\n")

# 步骤 4: 构建 APK
print("🔨 [4/5] 构建 APK（这需要 15-20 分钟）...")
!buildozer android debug
print("✅ APK 构建完成\n")

# 步骤 5: 下载 APK
print("📥 [5/5] 准备下载 APK...")
from google.colab import files
import os

apk_files = [f for f in os.listdir('bin') if f.endswith('.apk')]

if apk_files:
    apk_name = apk_files[0]
    print(f"✅ 找到 APK: {apk_name}")
    print(f"📦 文件大小: {os.path.getsize(f'bin/{apk_name}') / 1024 / 1024:.2f} MB")
    print("⬇️  开始下载...")
    files.download(f'bin/{apk_name}')
    print("\n" + "="*50)
    print("🎉 构建成功！APK 已下载到你的电脑")
    print("="*50)
    print("\n📱 下一步：")
    print("1. 将 APK 传输到 Android 手机")
    print("2. 在手机上点击 APK 文件")
    print("3. 允许安装未知来源应用")
    print("4. 点击安装")
    print("\n✨ 完成！")
else:
    print("❌ 错误：未找到 APK 文件")
    print("📋 查看构建日志:")
    !ls -la bin/
    print("\n可能的原因：")
    print("1. 构建失败 - 检查上面的错误信息")
    print("2. main.py 有语法错误")
    print("3. buildozer.spec 配置错误")
```

### 4. 运行脚本

点击单元格左侧的 ▶️ 播放按钮，或按 `Shift + Enter`

### 5. 等待构建完成

- **第 1 次构建**：约 25-30 分钟（需要下载 SDK）
- **后续构建**：约 15-20 分钟
- APK 会自动下载到你的电脑

### 6. 构建进度说明

```
[1/5] 安装 Python 依赖    ⏱️  2-3 分钟
[2/5] 克隆项目           ⏱️  1 分钟
[3/5] 安装系统依赖        ⏱️  3-5 分钟
[4/5] 构建 APK          ⏱️  15-20 分钟  ← 最耗时
[5/5] 下载 APK          ⏱️  1 分钟
```

---

## 安装测试

### 1. 传输 APK 到手机

方法选择：
- **USB 数据线**：直接复制到手机
- **云盘**：上传到网盘，手机下载
- **邮件**：发送到自己邮箱
- **即时通讯**：通过微信/QQ 发送

### 2. 安装 APK

1. 在手机上找到 APK 文件
2. 点击 APK 文件
3. 如果提示"未知来源"：
   - 点击"设置"
   - 允许安装未知来源应用
   - 返回继续安装
4. 点击"安装"
5. 等待安装完成
6. 点击"打开"测试应用

### 3. 测试应用

- 检查界面是否正常显示
- 测试所有功能是否正常
- 检查是否有崩溃或错误

---

## 自定义配置

### 修改应用名称和包名

编辑 `buildozer.spec`：

```ini
[app]
# 应用显示名称
title = 我的计算器

# 包名（只能包含字母、数字、下划线）
package.name = mycalculator

# 域名（反向域名格式）
package.domain = com.myname
```

### 添加应用图标

1. 准备图标文件 `icon.png`（建议 512x512 像素）
2. 放在项目根目录
3. 在 `buildozer.spec` 中配置：

```ini
icon.filename = %(source.dir)s/icon.png
```

### 添加启动画面

1. 准备启动画面 `presplash.png`（建议 1280x720）
2. 在 `buildozer.spec` 中配置：

```ini
presplash.filename = %(source.dir)s/presplash.png
```

### 添加 Python 依赖库

在 `buildozer.spec` 中：

```ini
# 添加需要的库，用逗号分隔
requirements = python3,kivy,requests,pillow,numpy
```

常用库：
- `requests` - HTTP 请求
- `pillow` - 图像处理
- `numpy` - 数值计算
- `sqlite3` - 数据库（内置）

### 设置应用权限

在 `buildozer.spec` 中：

```ini
# 添加需要的权限
android.permissions = INTERNET,WRITE_EXTERNAL_STORAGE,CAMERA
```

常用权限：
- `INTERNET` - 网络访问
- `WRITE_EXTERNAL_STORAGE` - 写入存储
- `CAMERA` - 相机
- `ACCESS_FINE_LOCATION` - GPS 定位

### 修改应用版本

```ini
version = 1.0.1
```

---

## 常见问题

### Q1: 构建失败怎么办？

**A:** 查看错误日志：

```python
# 在 Colab 中运行
!tail -100 .buildozer/android/platform/build-arm64-v8a/build.log
```

常见原因：
1. `main.py` 语法错误 → 本地先测试
2. 依赖库不兼容 → 检查 requirements
3. 网络问题 → 重新运行

### Q2: APK 没有下载？

**A:** 检查 bin 目录：

```python
!ls -la bin/
```

如果没有 APK：
1. 查看构建日志找错误
2. 确认构建完成（看到 "BUILD SUCCESSFUL"）
3. 手动查找 APK：

```python
!find . -name "*.apk"
```

### Q3: 应用安装后闪退？

**A:** 可能原因：
1. Python 代码有运行时错误
2. 缺少必要的权限
3. 依赖库版本不兼容

解决方法：
1. 本地先用 Kivy 测试
2. 检查 Android 日志（需要 USB 调试）
3. 简化代码逐步测试

### Q4: 可以在 Windows 本地构建吗？

**A:** 不推荐。原因：
- 需要安装 Java、Android SDK、NDK
- 配置复杂，容易出错
- Colab 免费且简单

如果一定要本地构建，建议使用 WSL2（Windows Subsystem for Linux）。

### Q5: 构建时间太长？

**A:** 优化方法：
1. 只构建一个架构：
```ini
android.archs = arm64-v8a
```

2. 使用 GitHub Actions 自动构建（后台运行）

3. 首次构建慢是正常的（下载 SDK），后续会快很多

### Q6: 如何更新应用？

**A:** 流程：
1. 修改 `main.py`
2. 更新版本号（`buildozer.spec` 中的 `version`）
3. `git push`
4. Colab 重新构建
5. 安装新 APK（会覆盖旧版本）

### Q7: 可以发布到 Google Play 吗？

**A:** 可以，但需要：
1. 构建 release 版本（不是 debug）
2. 签名 APK
3. 注册 Google Play 开发者账号（$25）
4. 遵守 Google Play 政策

### Q8: 支持哪些 Python 库？

**A:** 
- ✅ 纯 Python 库：大部分支持
- ✅ Kivy 生态：完全支持
- ⚠️ C 扩展库：部分支持（需要编译）
- ❌ 桌面 GUI 库：不支持（如 tkinter、PyQt）

### Q9: 应用体积太大？

**A:** 优化方法：
1. 只包含必要的库
2. 压缩图片资源
3. 移除未使用的代码
4. 使用 ProGuard 混淆（高级）

### Q10: 如何调试应用？

**A:** 方法：
1. **本地调试**：用 Kivy 在电脑上运行
2. **日志输出**：使用 `print()` 或 `Logger`
3. **USB 调试**：连接手机查看 logcat
4. **分步测试**：逐步添加功能

---

## 🎓 进阶技巧

### 使用 GitHub Actions 自动构建

创建 `.github/workflows/build.yml`：

```yaml
name: Build APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v4
    - uses: ArtemSBulgakov/buildozer-action@v1
      with:
        command: buildozer android debug
    - uses: actions/upload-artifact@v4
      with:
        name: apk
        path: bin/*.apk
```

每次 push 后，GitHub 会自动构建 APK！

### 添加数据库支持

```python
import sqlite3

# 创建数据库
conn = sqlite3.connect('myapp.db')
cursor = conn.cursor()

# 创建表
cursor.execute('''
    CREATE TABLE IF NOT EXISTS data (
        id INTEGER PRIMARY KEY,
        value TEXT
    )
''')

conn.commit()
conn.close()
```

### 网络请求示例

```python
import requests

# 添加到 requirements: requests
response = requests.get('https://api.example.com/data')
data = response.json()
```

---

## 📚 相关资源

- **Kivy 官方文档**: https://kivy.org/doc/stable/
- **Buildozer 文档**: https://buildozer.readthedocs.io/
- **Python for Android**: https://python-for-android.readthedocs.io/
- **Kivy 示例**: https://github.com/kivy/kivy/tree/master/examples

---

## 🆘 获取帮助

遇到问题？
1. 查看本文档的"常见问题"部分
2. 搜索 Kivy 官方文档
3. 在 GitHub Issues 提问
4. 加入 Kivy 社区

---

**准备好了？** 开始 [快速开始](./快速开始.md) 或查看 [Colab 构建脚本](./colab_build.py)

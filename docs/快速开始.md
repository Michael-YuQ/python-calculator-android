# 🚀 快速开始 - 3 步打包 APK

> 适合已经熟悉流程的快速参考

---

## 📝 第 1 步：写代码

编辑 `main.py`：

```python
from kivy.app import App
from kivy.uix.label import Label

class MyApp(App):
    def build(self):
        return Label(text='Hello World!')

if __name__ == '__main__':
    MyApp().run()
```

---

## 📤 第 2 步：推送到 GitHub

```bash
git add main.py
git commit -m "更新应用"
git push
```

---

## 🔨 第 3 步：Colab 构建

### 3.1 打开 Colab
https://colab.research.google.com/

### 3.2 新建笔记本
点击 "新建笔记本"

### 3.3 复制构建脚本
复制 [`colab_build.py`](./colab_build.py) 的内容到单元格

或者直接复制以下代码：

```python
# 配置你的仓库
GITHUB_REPO = "https://github.com/你的用户名/你的仓库名.git"
REPO_NAME = "你的仓库名"

# 构建脚本
!pip install -q buildozer cython
!git clone {GITHUB_REPO}
%cd {REPO_NAME}
!sudo apt-get update -qq
!sudo apt-get install -y -qq git zip unzip openjdk-17-jdk wget autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev
!buildozer android debug

from google.colab import files
import os
apk_files = [f for f in os.listdir('bin') if f.endswith('.apk')]
if apk_files:
    files.download(f'bin/{apk_files[0]}')
```

### 3.4 运行
点击 ▶️ 或按 `Shift + Enter`

### 3.5 等待
20-30 分钟后 APK 自动下载

---

## 📱 安装到手机

1. 传输 APK 到手机
2. 点击安装
3. 完成！

---

## 🔄 下次修改

1. 改 `main.py`
2. `git push`
3. Colab 重新运行
4. 下载新 APK

---

**需要详细说明？** 查看 [完整流程指南](./完整流程指南.md)
